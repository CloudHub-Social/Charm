name = "charm-validator"
description = "Charm CI-only validation role for this Mac."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "On this Mac, do not run local build, test, lint, formatting-check, benchmark, browser, docs-build, or graph rebuild commands. Identify the required GitHub Actions checks, verify the exact head SHA, and report CI, device, deployment, and live evidence separately."
