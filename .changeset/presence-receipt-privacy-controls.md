---
default: patch
---

Add presence and receipt privacy controls (hide read receipts, hide typing indicators, appear offline, auto-idle) behind a default-off `presence_privacy_controls` feature flag.
