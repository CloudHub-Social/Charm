---
"charm": patch
---

Avoid claiming a password change may have been dispatched when cancellation fails during the initial recovery-email request before any attempt reaches the browser. Preserve late-attempt cleanup and uncertainty warnings for issued attempts.
