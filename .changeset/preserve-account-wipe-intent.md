---
"charm": patch
---

Preserve full account-data deletion intent in a fallback marker when the primary marker cannot be written. Startup retries encrypted-store and search cleanup rather than downgrading deactivation to ordinary logout.
