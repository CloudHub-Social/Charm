---
"charm": patch
---

Keep native Matrix sessions signed in across ordinary access-token expiry by requesting, rotating, and durably persisting refresh tokens without allowing a logged-out or superseded client to overwrite the current account session.
