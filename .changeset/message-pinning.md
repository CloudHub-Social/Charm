---
default: patch
---

Add message pinning (shared, room-state `m.room.pinned_events`) — pin/unpin from the message action menu, a pinned-messages panel, and a header pin-count badge — behind a default-off `message_pinning` feature flag.
