<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Spec 11: required on API 33+ to actually show a push-triggered
         notification; requested from the frontend's notifications settings
         panel, same as the local-notification permission Spec 10 already
         requests. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- Spec 11: lets UnifiedPush.getDistributors() see installed external
         distributor apps despite Android 11+'s package-visibility
         restrictions — without this, every external distributor query comes
         back empty and this app would always fall back to the embedded FCM
         distributor even when the user has a real one installed. -->
    <queries>
        <intent>
            <action android:name="org.unifiedpush.android.distributor.REGISTER" />
        </intent>
        <intent>
            <action android:name="org.unifiedpush.android.distributor.UNREGISTER" />
        </intent>
    </queries>

    <application
        android:name=".CharmApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.charm"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:dataExtractionRules="@xml/secure_store_backup_rules"
        android:fullBackupContent="@xml/secure_store_backup_rules_legacy">
        <meta-data android:name="io.sentry.auto-init" android:value="false" />
        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            android:authorities="${applicationId}.SentryInitProvider"
            tools:node="remove" />
        <provider
            android:name="io.sentry.android.core.SentryPerformanceProvider"
            android:authorities="${applicationId}.SentryPerformanceProvider"
            tools:node="remove" />
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>

        <!-- Spec 11: receives every UnifiedPush event (new endpoint,
             registration failure, unregistration, incoming message) and
             forwards each to the Rust core over JNI — see
             `PushMessagingReceiver.kt`. Must be exported: the distributor
             delivering these broadcasts (when it's an external UnifiedPush
             app, not the embedded FCM fallback) is a *different* app, and
             Android only allows a non-exported component to receive
             broadcasts from within the same app — matches the connector
             library's own manifest snippet
             (org.unifiedpush.android.connector.MessagingReceiver's kdoc).
             Fixed after PR review: this was wrongly `false` and would have
             silently dropped every push from an external distributor. -->
        <receiver
            android:name=".PushMessagingReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.unifiedpush.android.connector.MESSAGE" />
                <action android:name="org.unifiedpush.android.connector.UNREGISTERED" />
                <action android:name="org.unifiedpush.android.connector.NEW_ENDPOINT" />
                <action android:name="org.unifiedpush.android.connector.REGISTRATION_FAILED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
